
#Область СлужебныйПрограммныйИнтерфейс

&Перед("ОбработчикКоманды")
Процедура ИТМ_ОбработчикКоманды(ФормаОтчета, Команда, Результат) Экспорт
	
	Попытка
		ИТМ_ИсторияФормированияОтчетовКлиент.ФормаОтчета_ОбработчикКоманды(
			ФормаОтчета, Команда, Результат);
	Исключение
		ИТМ_ИсторияФормированияОтчетовВызовСервера.ДобавитьЗаписьВЖурналРегистрации(
			Неопределено, "ФормаОтчета_ОбработчикКоманды", ОписаниеОшибки());
	КонецПопытки;

КонецПроцедуры

#КонецОбласти
